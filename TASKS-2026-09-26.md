# TryingOpen2API 终局闭环总审计 · 第 4 轮（2026-09-26）

> 依据：4 个只读审计子代理（A/B/C/D）报告（docs/audit/A4/B4/C4/D4）+ 主线程复核。
> 规范：先审计 → 分主题修复 → 每批门禁 + 真实验收 → 文档同步 → HTML 报告+测验。
> 仓库：lza6/Tryingopen-2api（main；HEAD 10f0247，v0.1.10）

## 修复批次（按主题 commit）
- Batch-1 安全/日志/BLOCKER：D1 log_request UTF-8 panic；B4 clear 权限 + generate 上限；B1 /metrics 鉴权；B2 鉴权前置/限流/连接；B3 直连兜底配额 + 配置默认；M4 key 上限；面板 key 注入修复（C MAJOR-1 联动）。
- Batch-2 协议正确性：A1 Anthropic 图片去重；A3 Anthropic error→message_stop；A2 非流式 error→HTTP 错误；A8 start_event model/block_index；A6/A7 截断首条+Anthropic 截断；A5 并发许可释放时机。
- Batch-3 前端/契约/UX：MAJOR-1 面板 API key 管理 UI；MAJOR-2 fetch 超时+loading；MAJOR-3 指南用 /api/guide；MAJOR-4 价格；MAJOR-5/6 表格兜底；MINOR 一批（a11y/toast/转义/防连点）。
- Batch-4 文档/CI/配置：版本统一；sqlite 空壳说明；INDEX/audit 台账；workflow 收敛；CD rollback + cancel-in-progress:false；config.example 对齐；README 口径统一（每日）。
- Batch-5 终局验收：门禁全绿 + 公网 E2E（healthz/UI/models/chat/Anthropic/responses/429/503/metrics）+ HTML 报告 + 测验。

## 真实验收矩阵（每批跑对应项）
- Batch-1：cargo fmt/clippy/test；单测：log_request 多字节 key 不 panic、clear 需确认、/metrics 401、限流按 key 上限。
- Batch-2：新增/更新单测：anthropic 图片不重复、error→message_stop、非流式 error→502、start_event model。
- Batch-3：面板 JS 走查 + 浏览器测默认 401 引导、指南真实地址、价格/表格兜底。
- Batch-4：文档引用一致性（rg 版本/端点）；CI 文件语法；compose config。
- Batch-5：全量门禁 + 公网 E2E（可跑项）+ 报告。

## 防重复台账
- 依赖 0 CVE / 密钥 0 泄漏 / fmt clippy 42 tests → Batch-1 后复跑；Batch-2/3/4 改动后按影响面复跑对应项。
- 公网 E2E 只在 Batch-5 统一跑（避免频繁刷上游配额）。
