# E2E 只读复核报告 — v0.1.13 目标 / 实际 v0.1.12 二进制

- 日期：2026-09-26（Asia/Shanghai）
- 项目：`C:\Users\Administrator.DESKTOP-EGNE9ND\Desktop\tryingopen-2api`
- 模式：只读 E2E 复核（除本报告外未修改任何源文件；无 git 操作）
- 被测二进制：`target\release\tryingopen2api.exe`（SHA256 `C079400D1E7F6AECFE9B92A22A2BACDE2E0DEABAA7BA111B62FDF3FACA997F05`）
- 启动方式：`--config target\review-config-2026-09-26.json`，`RUST_LOG=info`，端口 127.0.0.1:47831

## 结论

- 网关可正常启动并对外服务；核心链路（healthz / 模型目录 / 面板 / 鉴权 / 真实 chat / usage 计数 / 结构化脱敏日志）在本机复核中全部可用。
- **真实 chat 200 一次成功**：模型 `qwen/qwen3.8-27b`，返回内容 `\n\nREV_OK`，含 reasoning_content，耗时约 2.2s（上游真实响应，非伪造）。
- **阻塞性问题（阻断 v0.1.13 交付口径）**：`Cargo.toml` 已声明 `version = "0.1.13"`，但 release 二进制启动日志与 `/healthz` 均为 `0.1.12`——**源码与产物版本不一致，当前 release 产物不是 v0.1.13**。必须以 0.1.13 重新构建后再复核（本任务只读，未代改）。
- `/v1/models` 当前为**动态目录（24 个模型）**，kimi（moonshotai/kimi-k3）响应中**无 message_limit 字段** → 按任务规格记为「如实反映上游」；同目录下所有模型均无 message_limit（`has_message_limit_field=0`）。静态目录（内置 catalog）kimi 定义有 `message_limit=Some(5)`，但本次启动实际走动态目录，静态口径不适用。
- `/api/usage` 无 key 在**冷启动（api_keys 空且未访问面板）时为 200**；访问 `/ui` 注入会话 key 后鉴权生效，无 key 变 401、带会话 key 200 且计数正确。任务规格的「无 key 401」需在面板已注入会话 key 后才能复现；本机放行语义（api_keys 空）是既定行为。

## 验证矩阵

| # | 验证项 | 结果 | 证据 |
|---|--------|------|------|
| 1 | 网关启动（临时 config，端口 47831） | ✅ PASS | 进程 PID 46520；`Get-NetTCPConnection -LocalPort 47831 -State Listen` → OwningProcess 46520；stdout `HTTP 服务已启动: http://127.0.0.1:47831` |
| 2 | `/healthz` 200 + version=0.1.13 | ⚠️ PARTIAL（版本不符） | HTTP 200；body `{"app":"tryingopen2api","models":24,"ok":true,"proxies":0,"upstream":"https://www.tryingopen.com","version":"0.1.12"}` → **version=0.1.12，非 0.1.13** |
| 3 | `/v1/models` 200 且含 reasoning/message_limit 字段 | ⚠️ PARTIAL（字段存在性） | HTTP 200；models=24（动态目录）；24/24 模型均含 `reasoning` 字段（本次响应全部 `false`）；0/24 含 `message_limit` 字段。kimi（moonshotai/kimi-k3）响应：`{"context":"128k","context_window":131072,"id":"moonshotai/kimi-k3","label":"Kimi K3","price_per_mtok":8.5,"reasoning":false,"tools":true,"vision":true}` → 无 message_limit，记为「如实反映上游」 |
| 4 | `/api/usage` 无 key 401 | ⚠️ PARTIAL（需面板先注入会话 key） | 冷启动（api_keys 空、未访问 /ui）：HTTP 200 `{"current_key":"","ok":true,...,"usage":null}`（本机放行）；访问 /ui 注入 `sk-to-session-*` 后：无 key → HTTP 401（`无效的 API Key...`）；带会话 key → HTTP 200 |
| 5 | `/api/usage` 带 key 200 | ✅ PASS | 带 `sk-to-session-79f9ac13b3d644ecb46b9058c8636ac9`：HTTP 200，usage 计数见 #9 |
| 6 | `/ui` 200 且含「思考」列 | ✅ PASS | HTTP 200，len=20027；`has_thinking=True`（HTML 表头含「思考」） |
| 7 | 面板注入会话 key 后可访问 `/v1/models` | ✅ PASS | `/ui` HTML 注入 `sk-to-session-79f9ac13b3d644ecb46b9058c8636ac9`；以 `x-api-key` 调用 `/v1/models` → HTTP 200，models_count=24 |
| 8 | 真实 chat（qwen/qwen3.8-27b，"Reply with exactly: REV_OK"，max_tokens 20）→ 200 | ✅ PASS | HTTP 200，elapsed≈2.56s（网关日志 took_ms=2214）；响应 `{"choices":[{"finish_reason":"stop","index":0,"message":{"content":"\n\nREV_OK","reasoning_content":"The user wants me to reply with exactly \"REV_OK\"...","role":"assistant"}}],"id":"chatcmpl-1790417230","model":"qwen/qwen3.8-27b","usage":{"completion_tokens":28,"prompt_tokens":1599,"reasoning_tokens":23,"total_tokens":1627}}`（注：日志脱敏 detail `text=8 tok` 为 detail 截断表示，非模型 token 数；响应 usage 为真实模型计数）。**本次复核仅用 1 次上游配额** |
| 9 | `/api/usage` 显示该 key 计数 +1 | ✅ PASS | chat 后查询：`{"current_key":"sk-to-session-79f9ac13b3d644ecb46b9058c8636ac9","usage":{"duration_ms_total":774,"errors_4xx":0,"errors_5xx":0,"ok":1,"rate_limited":0,"requests":1,"upstream_errors":0},"summary":{"tracked_keys":1,"total_ok":1,"total_requests":1}}` → requests=1, ok=1 |
| 10 | 结构化日志 REQ JSON（key 脱敏） | ✅ PASS | stdout 两条 `REQ` JSON 行：401 行 `"key":"sk-r***0926"`、200 行 `"key":"sk-t***6ac9"`（真实 key `sk-review-20260926` / `sk-to-session-79f9ac13b3d644ecb46b9058c8636ac9` 均被掩码为头尾 4 字符+`***`），且 200 行 detail 无明文 key |

## 阻塞

1. **版本漂移（硬阻塞）**：`Cargo.toml:3` 为 `version = "0.1.13"`，但 `target\release\tryingopen2api.exe` 启动日志与 `/healthz` 均为 `version 0.1.12`（Cargo.toml 声明 0.1.13 的改动未进入当前 release 产物）。v0.1.13 的上游验证结论不能建立在 0.1.12 二进制上，需重建 release 后再跑本矩阵。
2. `/v1/models` 本次为动态目录（24 模型），无 message_limit 字段，属于上游如实反映；若验收要求静态目录含 message_limit（kimi=5），需 `catalog_refresh_min>0` 或抓取失败保留静态目录场景，与本次配置（0 关闭动态刷新但启动仍抓取成功）不一致。已按规格标注「如实反映上游」。

## 环境与执行说明

- 临时配置 `target\review-config-2026-09-26.json`：api_keys=[]、free_proxy_enabled=false、catalog_refresh_min=0、skip_upstream_check=true、proxy_file=""、direct_fallback=true；未显式列出的字段取默认值（rate_limit_enabled=true 等）。工作目录存在 `config.local.json`？否（`Test-Path` = False）。
- 首次尝试以外部 key `sk-review-20260926` 调 chat 返回 401，原因是 `/ui` 已注入会话 key 后运行时鉴权生效（外部 key 不在允许表）；改用面板注入的会话 key 后真实 chat 200。任务规格「Bearer 任意 key」与实现（动态会话 key 表 + 空 api_keys 放行）存在语义差异，已如实记录。
- 测试结束后已 `Stop-Process` 停止网关，端口 47831 无残留监听、无残留进程。
- 日志文件：`target\review-gateway.stdout.log` / `target\review-gateway.stderr.log`（临时产物，未提交）。
- 未执行任何 git 操作；未修改任何源文件（唯一写入为本报告 + 临时 target 文件）。
