# E2E 真实验收（公网生产）— 2026-09-26

> 目标：https://try.hwhcie.bond（nginx HTTPS → 127.0.0.1:47831 systemd）
> 部署版本：v0.1.11（CD 自动部署，CD-Deploy 36187645344 success，4m7s）
> 执行方式：主控代理直接 HTTP 实测（直连公网，绕过本机代理）；无生产 API key / ui_password
> 说明：带 key 的调用类检查（chat/messages/responses 200）需生产 key，本报告如实记录为「待补跑」；无 key 探活与安全拒绝为真实实测。

## 真实实测结果（无 key / 无凭据）

| # | 检查 | 真实状态 | 结论 |
|---|---|---|---|
| T1 | GET /healthz | 200 | ✅ PASS（body: `{"app":"tryingopen2api","models":24,"ok":true,"proxies":4500,"version":"0.1.11"}`） |
| S1 | GET /ui（无 Basic Auth） | 401 | ✅ PASS（安全拒绝：ui_password 已配置） |
| S2 | GET /v1/models（无 key） | 401 | ✅ PASS（安全拒绝） |
| S3 | GET /metrics（无 key） | 401 | ✅ PASS（安全拒绝，本轮 B1 修复生效） |
| S4 | POST /v1/chat/completions（无 key） | 401 | ✅ PASS（安全拒绝） |
| S5 | POST /v1/messages（无 key） | 401 | ✅ PASS（安全拒绝） |
| S6 | POST /v1/responses（无 key） | 401 | ✅ PASS（安全拒绝） |

带 key 检查（T3b/T4-T10）：**SKIP** — 需要生产 API key（config.json 的 api_keys / 面板生成 key），未提供前不臆造结果；脚本 `scripts/e2e_smoke.sh` 已就绪，`API_KEY=<生产key> scripts/e2e_smoke.sh` 可补跑。

## 发布验收（独立证据）

- CI：main push 36187645340 ✅ success（fmt/clippy/test/security）
- CD-Deploy：main push 36187645344 ✅ success（4m7s，自动部署到 Azure）
- Release：v0.1.11 36187695234 ✅ success（4m9s）
- GitHub Release v0.1.11：✅ 已发布，资产 `tryingopen2api.exe` + `tryingopen2api.exe.sha256`
- 公网 healthz：✅ version=0.1.11 / models=24 / proxies=4500

## 结论

- 生产已运行 v0.1.11，本轮安全加固（metrics 鉴权/面板 Basic Auth/全端点 key 鉴权）在公网真实生效：无凭据一律 401。
- 带 key 的调用类 E2E 待生产 key 提供后补跑（脚本已备好）。
